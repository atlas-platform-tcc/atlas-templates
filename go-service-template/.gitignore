# Go build output (binary is named after the module) and test artifacts.
/hello-service
*.test
*.out

# Local dev overrides for docker compose — never commit credentials (.env.example is the template).
.env
